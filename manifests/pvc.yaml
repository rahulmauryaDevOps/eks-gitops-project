apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: nginx-pvc
  namespace: nginx-project

spec:
  storageClassName: gp2

  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 1Gi